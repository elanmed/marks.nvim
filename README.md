# `marks.nvim`

A set of utilities to smooth out native vim marks.

## Sample configuration

```lua 
vim.g.marks = {
  -- defaults:
  remap_m = true,
  notify = true,
}
```

## Configuration options

#### `vim.g.marks.remap_m`
- TODO

#### `vim.g.marks.notify`
- TODO
